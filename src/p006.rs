pub fn p006() {
    /*
    just do the maths...
    (1 + ... + 100)^2:
        (1..=100).sum::<u32>().pow(2)
    (1^2 + ... + 100^2):
        (1..=100).map(|x| x * x).sum::<u32>()
    */
    println!(
        "{}",
        (1..=100).sum::<u32>().pow(2) - (1..=100).map(|x| x * x).sum::<u32>()
    );
}
